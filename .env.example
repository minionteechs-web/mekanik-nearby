VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000

# Production: set to your deployed API origin
# PUBLIC_URL is configured on the backend for chat media URLs

# Optional: Mapbox token for premium map tiles (future upgrade)
# VITE_MAPBOX_TOKEN=your_mapbox_token
